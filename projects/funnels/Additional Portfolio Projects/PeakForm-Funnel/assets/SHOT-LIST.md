# PeakForm Coaching — Shot List

Every image slot on the page. Drop the real file into `assets/` with the exact
filename and it wires up automatically (no code changes needed). Palette-tinted
atmospheric gradients stand in until each file arrives.

| # | Slot | Filename | Subject brief |
|---|------|----------|---------------|
| 1 | Hero section background (full-bleed) | `bg-hero.jpg` | Wide, atmosphere-first training environment: a client mid-workout (dumbbells, garage gym, or home setup), environment leading, face secondary, motion energy, soft focus acceptable. Shot for a dark tinted scrim, so contrast and mood matter more than sharpness. |
| 2 | Proof grid, transformation card 1 | `proof-transformation-1.jpg` | Real client before/after progress photo (with signed permission). Side-by-side or split frame, consistent lighting between shots. Portrait orientation preferred (card is tall). |
| 3 | Proof grid, transformation card 2 | `proof-transformation-2.jpg` | Second real client before/after progress photo (with signed permission). Landscape or square is fine here. |
| 4 | Coach section background band (full-bleed, heavy scrim) | `bg-coach-band.jpg` | Candid coaching-context photo: Marcus reviewing a client check-in on a laptop or phone, or demonstrating a movement. Sits under a ~90% lavender scrim, so it only needs to read as texture. |
| 5 | Coach card photo | `coach-marcus-action.jpg` | Candid, high-energy photo of Marcus Reed mid-session or coaching, genuine expression, not a stiff studio headshot. Head and shoulders to waist-up crop. |
| 6 | Close section background (full-bleed, strong volt scrim) | `bg-close.jpg` | Same image family as the hero background (training environment, energy, movement), different frame. Sits under a ~90% volt-yellow scrim, so silhouette and texture are what survive. |
| 7 | Nav logo | `logo.svg` | Client's existing logo file. Currently an Anton-set "PEAKFORM" wordmark stands in; swap the `<a class="nav__logo">` text for an `<img>` when the file arrives. |

Notes:
- All backgrounds render beneath palette-tinted gradient scrims already in the CSS; supply images with usable mid-tones and they will read correctly.
- Transformation photos must be the client-supplied ones with permission. Do not substitute stock for slots 2 and 3.
